import { CutomedirectivesDirective } from './cutomedirectives.directive';

describe('CutomedirectivesDirective', () => {
  it('should create an instance', () => {
    const directive = new CutomedirectivesDirective();
    expect(directive).toBeTruthy();
  });
});
